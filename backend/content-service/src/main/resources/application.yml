server:
  port: 8082

spring:
  application:
    name: content-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/content_service
      database: content_service

logging:
  level:
    com.aigrowth: DEBUG
    org.springframework.data.mongodb: DEBUG