server:
  port: 8083

spring:
  application:
    name: content-service
  data:
    mongodb:
      uri: mongodb://aigrowth:aigrowth123@localhost:27017/content_service?authSource=admin
      database: content_service

logging:
  level:
    com.aigrowth: DEBUG
    org.springframework.data.mongodb: DEBUG